# 微信小程序上传前检查报告

日期：2026-05-24
项目：住港伴 V4（V4.2.0）
AppID：wx08c2222c1bf042fd
基础库版本：3.15.2

---

## 检查结果汇总

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 一、域名配置（合法域名） | ⚠️ 需确认 | 2个外域需人工核对白名单 |
| 二、业务域名配置 | ✅ 不适用 | 未使用 web-view 组件 |
| 三、AppID 检查 | ✅ 通过 | project.config.json 已配置 |
| 四、环境清理（调试代码） | ⚠️ 建议清理 | 大量 console.* 存在于源代码 |
| 四、环境清理（Mock数据） | ✅ 通过 | Mock 仅限测试目录（已排除上传） |
| 四、环境清理（API地址） | ✅ 通过 | 全部指向生产环境 HTTPS |
| 五、上传后验证 | 📋 提醒 | 需在后台确认版本信息 |
| 六、隐私协议 | 🔴 未通过 | app.json 缺少 __usePrivacyCheck__ |
| 六、盲测 | 📋 提醒 | 需完成最后一次全功能走查 |
| 七、版本更新日志 | ✅ 通过 | CHANGELOG_V4.md + RELEASE_NOTES_V4.2.md |

**结论：存在 1 项阻塞（隐私协议），需修复后再上传；2 项建议修复。**

---

## 一、域名配置检查

### 发现的域名与用途

| 域名 | 用途 | 调用方式 | HTTPS | 需在白名单 |
|------|------|----------|-------|-----------|
| api.zhugangban.com | 主业务 API (/v1/*) | wx.request (api.js:24) | ✅ | 🔶 需确认 |
| cloudbase-d1g17tgt7cc199a60.service.tcloudbase.com | AI 流式对话 HTTP | wx.request (api.js:210) | ✅ | 🔶 需确认 |
| (动态 imagePath) | 分享图片下载保存 | wx.downloadFile (share-preview:124) | ⚠️ 动态 | 🔶 需关注 |

### 无需白名单的调用
- `wx.cloud.callFunction(...)` — 云函数调用走微信云通道，免配域名
- `wx.requestPayment(...)` — 微信支付内置 API
- `wx.requestSubscribeMessage(...)` — 订阅消息内置 API

### 操作提醒
请登录 [微信公众平台](https://mp.weixin.qq.com/) → 开发 → 开发设置 → 服务器域名，确认 **request 合法域名** 列表中已包含：
1. `https://api.zhugangban.com`
2. `https://cloudbase-d1g17tgt7cc199a60.service.tcloudbase.com`

**注意：** `share-preview` 页面使用 `wx.downloadFile` 下载动态图片 URL，若图片来源为第三方域名，需同步加入 **downloadFile 合法域名** 白名单。

---

## 二、业务域名配置检查

项目中未搜索到 `<web-view>` 组件使用。

✅ 此项不适用。

---

## 三、AppID 检查

- **project.config.json**: `"appid": "wx08c2222c1bf042fd"`
- **cloudbaserc.json**: `"envId": "cloudbase-d1g17tgt7cc199a60"`

🔶 请确认以下内容：
1. 当前微信开发者工具登录账号对应的小程序后台 AppID 为 `wx08c2222c1bf042fd`
2. 该小程序已绑定 CloudBase 环境 `cloudbase-d1g17tgt7cc199a60`

---

## 四、环境清理

### 4.1 调试代码（console.* / debugger）

源代码中存在大量 `console.log/debug/warn/error` 调用。以下是按文件汇总（仅统计将被上传的源代码，不含 scripts/、tests/、coverage/、node_modules/）：

| 文件 | console.debug | console.warn | console.error | 建议 |
|------|:---:|:---:|:---:|------|
| app.js | 7 | 1 | 5 | 保留 warn/error，清理 debug |
| utils/api.js | 0 | 3 | 7 | 保留 warn/error，关注是否有敏感信息 |
| utils/storage.js | 0 | 5 | 1 | 均为异常处理日志，可保留 |
| utils/ocr.js | 0 | 0 | 1 | 可保留 |
| utils/event-tracker.js | 0 | 1 | 0 | 可保留 |
| utils/pdf-generator.js | 0 | 0 | 1 | 可保留 |
| components/floating-ai/floating-ai.js | 0 | 1 | 1 | 可保留 |
| pages/guidebooks/index/index.js | 待确认 | 待确认 | 待确认 | — |
| subpkg-chat/pages/membership/index.js | 0 | 0 | 2 | 可保留 |
| subpkg-docs/pages/documents-add/index.js | 3 | 2 | 3 | 部分可清理 |
| subpkg-low/pages/assessment-index/index.js | 1 | 0 | 1 | 可保留 |
| subpkg-low/pages/admin-*  | 1 | 0 | 1 | 可保留 |
| subpkg-feedback/pages/wecom-qr/index.js | 2 | 0 | 1 | 可保留 |

**建议：**
- 🔶 **至少清理所有 `console.debug`**（约 15+ 处），这些面向开发调试的日志不应出现在生产版本
- ✅ `console.error` / `console.warn` 多为异常处理，可保留用于线上故障排查
- ✅ 未发现 `debugger` 语句
- ✅ `project.config.json` 中 `"uploadWithSourceMap": false` 已正确设置

### 4.2 关闭 Mock 数据

- ✅ Mock 仅出现在 `__tests__/` 目录（`global.__mockStorage`），该目录已通过 packOptions 排除上传
- ✅ API base URL 全部指向生产环境：
  - 主 API：`https://api.zhugangban.com/v1`
  - AI 流式：`https://cloudbase-d1g17tgt7cc199a60.service.tcloudbase.com/ai-chat`
- ✅ 未发现 `localhost`、内网 IP、开发环境 URL
- ✅ 未发现 `isDev`/`isDebug`/`ENV_MODE` 等环境判断变量影响线上行为
- ℹ️ `.env.local` 中 `ENV_ID` 为空，但该文件为本地开发用，不影响上传包

### 4.3 上传排除项确认

`project.config.json` 中 `packOptions.ignore` 已正确排除以下非必要内容：
- cloudfunctions/、node_modules/、tests/、scripts/、inbox/、outbox/
- .hermes/、__tests__/、docs/
- 所有 .md 文件、.cjs/.mjs 文件
- cloudbaserc.json、package-lock.json、ledger.jsonl
- data/tasks/

✅ 包体积控制策略合理。

---

## 五、上传后验证提醒

上传完成后，请按以下步骤验证：

1. 登录 [微信公众平台](https://mp.weixin.qq.com/) → 管理 → 版本管理 → 开发版本
2. 确认刚上传的版本正常显示：
   - 版本号、上传时间正确
   - 代码包大小在合理范围（主包 < 2MB，分包 < 2MB）
   - 无缺失文件警告
3. 如显示异常，回退到开发者工具检查后重新上传

---

## 六、提交审核前最后一步

### 6.1 隐私协议 🔴

**问题：`app.json` 中未配置 `__usePrivacyCheck__: true`**

当前 `app.json` 中不存在此配置项。根据微信最新隐私保护要求，建议：

```json
{
  "__usePrivacyCheck__": true
}
```

将此配置添加到 `app.json` 根级别，与 `pages`、`window` 等字段并列。

同时确认：
- 小程序后台 → 设置 → 服务内容声明 → 用户隐私保护指引 已完成配置
- 隐私协议内容覆盖：收集的用户信息类型、使用目的、保存期限、用户权利

### 6.2 盲测

通过开发版本生成的二维码，完成最后一次全功能走查，重点测试：

- [ ] 登录流程（微信授权 + 手机号绑定）
- [ ] 支付流程（攻略书购买 / 会员订阅）
- [ ] 双闸门决策保护（5个入口）
- [ ] AI 对话（流式渲染、快捷回复）
- [ ] 证件夹（上传 + OCR 识别）
- [ ] 提醒器（路径切换封存/解封）
- [ ] 流程控（路径选择 + 里程碑）
- [ ] 分享功能
- [ ] 反馈模块
- [ ] 异常场景（网络断开、超时、token 过期）

---

## 七、版本更新日志

- ✅ `CHANGELOG_V4.md` — 完整技术变更记录
- ✅ `RELEASE_NOTES_V4.2.md` — 包含微信审核发版文案
- 建议在上传时，将 `RELEASE_NOTES_V4.2.md` 中的文案填入审核备注

---

## 结论

| 状态 | 数量 | 说明 |
|------|:----:|------|
| 🔴 阻塞 | 1 | 缺少 `__usePrivacyCheck__` 配置 |
| 🔶 建议修复 | 2 | console.debug 清理；域名白名单人工确认 |
| ✅ 通过 | 5 | 业务域名、AppID、Mock数据、API地址、版本日志 |
| 📋 提醒 | 2 | 上传后验证、盲测 |

**当前：不建议直接上传。请先修复隐私协议配置，再清理 console.debug，确认域名白名单后进行上传。**
