.header-title {
  font-size: var(--font-xl);
  font-weight: 700;
  color: var(--color-bg-card);
  display: block;
}
.header-sub {
  font-size: var(--font-xs);
  opacity: 0.75;
  display: block;
  margin-top: 4rpx;
}
