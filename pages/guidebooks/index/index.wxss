/* 攻略书 v7 — 港漂通关手册 (DSG-3 令牌迁移) */

/* ===== Tab Bar ===== */
.tab-bar {
  display: flex;
  background: var(--white);
  border-bottom: 1rpx solid var(--gray-200);
  position: sticky;
  top: 0;
  z-index: var(--z-sticky);
}
.tab-item {
  flex: 1;
  text-align: center;
  padding: 20rpx 0 16rpx;
  font-size: var(--font-md);
  color: var(--gray-400);
  position: relative;
  transition: color var(--duration-fast) var(--easing);
}
.tab-item.active {
  color: var(--blue);
  font-weight: 600;
}
.tab-indicator {
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 48rpx;
  height: 6rpx;
  background: var(--blue);
  border-radius: 3rpx;
}

/* ===== Loading & Error ===== */
.loading-wrap { padding: 40rpx; }
.error-wrap {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 120rpx 60rpx;
}
.error-icon { font-size: 80rpx; margin-bottom: 24rpx; }
.error-text { font-size: var(--font-md); color: var(--gray-400); margin-bottom: 32rpx; }
.btn-retry {
  padding: 16rpx 48rpx;
  background: var(--blue);
  color: var(--white);
  border-radius: 40rpx;
  font-size: var(--font-md);
  font-weight: 600;
}

/* ===== Current Phase Indicator ===== */
.current-phase-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12rpx 32rpx;
  background: var(--gray-50);
  border-bottom: 1rpx solid var(--gray-100);
}
.cp-label {
  font-size: var(--font-xs);
  font-weight: 600;
  color: var(--blue);
}
.cp-dots {
  display: flex;
  gap: 8rpx;
}
.cp-dot {
  width: 12rpx;
  height: 12rpx;
  border-radius: 50%;
  background: var(--gray-200);
  transition: background var(--duration-fast);
}
.cp-dot.done { background: var(--green); }
.cp-dot.active { background: var(--blue); width: 20rpx; border-radius: 6rpx; }

/* ===== Dossier Bar (Tab 0 top) ===== */
.dossier-bar {
  display: flex;
  align-items: center;
  padding: 16rpx 32rpx;
  background: linear-gradient(135deg, var(--blue-light), #e0ecff);
  margin: 16rpx;
  border-radius: var(--radius);
  transition: transform var(--duration-fast);
}
.dossier-bar:active { transform: scale(0.98); }
.dossier-label {
  font-size: var(--font-sm);
  font-weight: 600;
  color: var(--blue);
  margin-right: 24rpx;
}
.dossier-stat { font-size: var(--font-xs); color: var(--gray-500); margin-right: 16rpx; }
.dossier-arrow { font-size: 32rpx; color: var(--gray-400); margin-left: auto; }

/* ===== Phase List ===== */
.phase-list { height: calc(100vh - 200rpx); padding: 0 16rpx; }
.phase-card {
  margin-bottom: 12rpx;
  background: var(--white);
  border-radius: var(--radius);
  overflow: hidden;
  box-shadow: var(--shadow-sm);
  transition: opacity var(--duration-fast);
}
.phase-card.locked {
  opacity: 0.55;
  background: var(--gray-100);
  box-shadow: none;
  border: 1rpx dashed var(--gray-300);
}
.phase-card.locked .phase-header {
  color: var(--gray-400);
}
.phase-card.locked .phase-name {
  color: var(--gray-500);
}
.phase-card.locked .task-item {
  pointer-events: none;
}
.phase-card.locked .task-item text {
  color: var(--gray-400);
}
.lock-icon {
  font-size: var(--font-lg);
  margin-left: 8rpx;
}
.phase-lock-hint {
  padding: 16rpx 28rpx;
  background: var(--gray-50);
  border-top: 1rpx solid var(--gray-200);
  font-size: var(--font-xs);
  color: var(--gray-500);
  text-align: center;
}

/* ===== ¥9.90 Unlock Banner ===== */
.unlock-banner {
  margin: 0 28rpx 16rpx;
  padding: 24rpx 28rpx;
  background: linear-gradient(135deg, #EFF6FF 0%, #F5F3FF 100%);
  border-radius: var(--radius);
  border: 1rpx solid var(--blue-light);
}
.unlock-banner__options {
  display: flex;
  gap: 16rpx;
}
.unlock-option {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  padding: 20rpx 16rpx;
  background: var(--white);
  border-radius: var(--radius-sm);
  text-align: center;
}
.unlock-option--quick {
  border: 2rpx solid var(--color-primary);
}
.unlock-option__title {
  font-size: var(--font-md);
  font-weight: 600;
  color: var(--gray-800);
}
.unlock-option__desc {
  font-size: var(--font-xs);
  color: var(--gray-500);
  line-height: 1.4;
}
.unlock-option .btn {
  margin-top: 8rpx;
  width: 100%;
}
.phase-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 28rpx;
  transition: background var(--duration-fast);
}
.phase-header:active { background: var(--gray-50); }
.phase-left { display: flex; align-items: center; gap: 16rpx; }
.phase-icon { font-size: 36rpx; }
.phase-name { font-size: var(--font-md); font-weight: 600; color: var(--color-text); }
.phase-progress { font-size: var(--font-xs); color: var(--gray-400); }
.phase-toggle { font-size: var(--font-xs); color: var(--gray-300); }

/* ===== Task Cards ===== */
.task-list { border-top: 1rpx solid var(--gray-100); }
.task-card { border-bottom: 1rpx solid var(--gray-50); }
.task-card.done-full { background: var(--color-success-bg); }
.task-card.done-steps { background: var(--color-success-bg); }
.task-card.skipped { opacity: 0.4; }
.task-collapsed {
  display: flex;
  align-items: center;
  padding: 20rpx 28rpx;
  gap: 16rpx;
  transition: background var(--duration-fast);
}
.task-collapsed:active { background: var(--gray-50); }
.task-status { font-size: 34rpx; color: var(--blue); width: 50rpx; text-align: center; }
.task-body { flex: 1; overflow: hidden; }
.task-title {
  font-size: var(--font-md);
  color: var(--gray-700);
  display: block;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.task-subtitle {
  font-size: var(--font-xs);
  color: var(--gray-400);
  display: block;
  margin-top: 4rpx;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.task-meta { display: flex; flex-direction: column; align-items: flex-end; gap: 4rpx; }
.task-urgency { font-size: 20rpx; padding: 2rpx 10rpx; border-radius: 8rpx; }
.urgency-required { background: var(--color-error-bg); color: var(--color-error); }
.urgency-suggest { background: var(--color-warning-bg); color: var(--color-warning); }
.urgency-optional { background: var(--gray-100); color: var(--gray-500); }
.task-time { font-size: var(--font-xs); color: var(--gray-300); }

/* ===== Task Expanded ===== */
.task-expanded { padding: 0 28rpx 20rpx; }
.expanded-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20rpx 0 16rpx;
  border-bottom: 1rpx solid var(--gray-100);
}
.expanded-title { font-size: var(--font-md); font-weight: 600; color: var(--color-text); }
.expand-close { font-size: var(--font-xs); color: var(--blue); }
.steps-list { padding: 16rpx 0; }
.step-item { display: flex; align-items: flex-start; padding: 12rpx 0; gap: 16rpx; }
.step-check {
  width: 44rpx;
  height: 44rpx;
  border: 2rpx solid var(--gray-300);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--font-xs);
  color: var(--gray-300);
  flex-shrink: 0;
  margin-top: 2rpx;
  transition: all var(--duration-fast) var(--easing);
}
.step-check.checked {
  background: var(--blue);
  border-color: var(--blue);
  color: var(--white);
}
.step-info { flex: 1; }
.step-title { font-size: var(--font-sm); font-weight: 500; color: var(--gray-600); display: block; }
.step-content {
  font-size: var(--font-xs);
  color: var(--gray-500);
  display: block;
  margin-top: 4rpx;
  line-height: 1.6;
  word-break: break-word;
}
.material-prompt {
  margin-top: 16rpx;
  padding: 16rpx 20rpx;
  background: var(--color-warning-bg);
  border-radius: 12rpx;
  font-size: var(--font-sm);
  color: var(--color-warning);
  text-align: center;
  transition: background var(--duration-fast);
}
.material-prompt:active { background: #fff3cd; }

/* ===== Tab 1: Browse / 场景速查 ===== */
.category-scroll { white-space: nowrap; padding: 16rpx; }
.category-tag {
  display: inline-block;
  padding: 10rpx 24rpx;
  margin-right: 12rpx;
  background: var(--gray-100);
  border-radius: 32rpx;
  font-size: var(--font-sm);
  color: var(--gray-600);
  transition: all var(--duration-fast);
}
.category-tag.active {
  background: var(--blue);
  color: var(--white);
  font-weight: 600;
}
.browse-list { height: calc(100vh - 200rpx); padding: 0 16rpx; }
.browse-card {
  padding: 24rpx;
  margin-bottom: 12rpx;
  background: var(--white);
  border-radius: 12rpx;
  box-shadow: var(--shadow-sm);
  transition: transform var(--duration-fast);
}
.browse-card:active { transform: scale(0.98); }
.browse-title { font-size: var(--font-md); font-weight: 600; color: var(--color-text); display: block; }
.browse-sub { font-size: var(--font-xs); color: var(--gray-400); display: block; margin: 6rpx 0; }
.browse-meta { display: flex; gap: 12rpx; margin-top: 8rpx; }
.tag-sm {
  font-size: 20rpx;
  padding: 2rpx 10rpx;
  background: var(--gray-50);
  border-radius: 6rpx;
  color: var(--gray-500);
}
.tag-sm.scene {
  background: var(--blue-light);
  color: var(--blue);
}

/* Browse search bar */
.browse-search-bar {
  padding: 12rpx 16rpx;
  background: var(--white);
  border-bottom: 1rpx solid var(--gray-100);
}
.browse-search-input {
  background: var(--gray-50);
  border-radius: 32rpx;
  padding: 14rpx 28rpx;
  font-size: var(--font-md);
  height: 64rpx;
}

/* Browse card layout — two-column: left(title+sub) + right(urgency+time+status) */
.browse-card-main {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
}
.browse-card-left { flex: 1; min-width: 0; }
.browse-card-right {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 6rpx;
  flex-shrink: 0;
  margin-left: 16rpx;
}
.browse-done-icon { font-size: 28rpx; color: var(--green); }
.browse-warn-icon { font-size: 28rpx; color: var(--warning, #f0ad4e); }
.browse-expand-icon { font-size: 20rpx; color: var(--gray-400); }

/* Structured detail panel */
.browse-detail {
  margin-top: 20rpx;
  padding-top: 16rpx;
  border-top: 1rpx solid var(--gray-100);
}
.browse-section { margin-bottom: 20rpx; }
.browse-section-title {
  font-size: var(--font-sm);
  font-weight: 600;
  color: var(--color-text);
  display: block;
  margin-bottom: 10rpx;
}
.browse-step {
  display: flex;
  gap: 8rpx;
  margin-bottom: 14rpx;
  padding-left: 4rpx;
}
.browse-step-num {
  font-size: var(--font-xs);
  color: var(--gray-400);
  font-weight: 600;
  width: 28rpx;
  flex-shrink: 0;
}
.browse-step-body { flex: 1; }
.browse-step-title {
  font-size: var(--font-sm);
  font-weight: 500;
  color: var(--color-text);
  display: block;
}
.browse-step-content {
  font-size: var(--font-xs);
  color: var(--gray-500);
  display: block;
  margin-top: 4rpx;
  line-height: 1.5;
  word-break: break-word;
}
.browse-item-row {
  font-size: var(--font-xs);
  color: var(--gray-500);
  padding: 4rpx 0;
  line-height: 1.5;
}

/* Browse action buttons */
.browse-actions {
  margin-top: 16rpx;
  padding-top: 16rpx;
  border-top: 1rpx solid var(--gray-100);
  text-align: center;
}
.browse-action-row {
  display: flex;
  gap: 16rpx;
  justify-content: center;
  align-items: center;
}
.browse-done-tag {
  font-size: var(--font-sm);
  color: var(--green);
  font-weight: 500;
  padding: 12rpx;
}
.browse-add-btn {
  display: inline-block;
  font-size: var(--font-sm);
  color: var(--white);
  background: var(--blue);
  padding: 14rpx 40rpx;
  border-radius: 32rpx;
  font-weight: 500;
}
.browse-add-btn:active { opacity: 0.8; }
.browse-detail-btn {
  display: inline-block;
  font-size: var(--font-sm);
  color: var(--blue);
  background: var(--blue-light);
  padding: 14rpx 32rpx;
  border-radius: 32rpx;
  font-weight: 500;
  border: 2rpx solid var(--blue);
}
.browse-detail-btn:active { opacity: 0.7; }

/* ===== Tab 2: Progress / 我的进度 ===== */
.progress-scroll { height: calc(100vh - 140rpx); padding: 16rpx; }
.stats-panel {
  background: var(--white);
  border-radius: var(--radius);
  padding: 28rpx;
  margin-bottom: 20rpx;
  box-shadow: var(--shadow-sm);
}
.stats-title {
  font-size: var(--font-lg);
  font-weight: 700;
  color: var(--color-text);
  display: block;
  margin-bottom: 20rpx;
}
.phase-row {
  display: flex;
  align-items: center;
  margin-bottom: 12rpx;
  gap: 12rpx;
}
.phase-row-name { width: 160rpx; font-size: var(--font-xs); color: var(--gray-500); }
.phase-bar-bg {
  flex: 1;
  height: 12rpx;
  background: var(--gray-100);
  border-radius: 6rpx;
  overflow: hidden;
}
.phase-bar-fill {
  height: 100%;
  background: var(--gradient-hero);
  border-radius: 6rpx;
  transition: width var(--duration-slow) var(--easing);
}
.phase-row-num { width: 60rpx; font-size: var(--font-xs); color: var(--gray-400); text-align: right; }

.dossier-panel {
  background: var(--white);
  border-radius: var(--radius);
  padding: 28rpx;
  box-shadow: var(--shadow-sm);
}
.dossier-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20rpx;
}
.dossier-title { font-size: var(--font-lg); font-weight: 700; color: var(--color-text); }
.dossier-btn {
  font-size: var(--font-sm);
  color: var(--blue);
  padding: 8rpx 20rpx;
  border: 1rpx solid var(--blue);
  border-radius: 24rpx;
}
.dossier-section {
  margin-bottom: 20rpx;
  padding-bottom: 16rpx;
  border-bottom: 1rpx solid var(--gray-100);
}
.dossier-cat {
  font-size: var(--font-md);
  font-weight: 600;
  color: var(--gray-600);
  display: block;
  margin-bottom: 10rpx;
}
.dossier-item { padding: 8rpx 0; font-size: var(--font-sm); color: var(--gray-600); }
.dossier-empty { font-size: var(--font-xs); color: var(--gray-300); padding: 8rpx 0; }

.empty-hint { padding: 80rpx 0; text-align: center; font-size: var(--font-md); color: var(--gray-300); }

/* ===== Path Setup Overlay / 路径设置弹窗 ===== */
.overlay {
  position: fixed;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0,0,0,0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: var(--z-overlay);
}
.setup-dialog {
  width: 640rpx;
  background: var(--white);
  border-radius: var(--radius-lg);
  padding: 48rpx 40rpx;
  animation: fadeIn var(--duration-slow) var(--easing);
}
.setup-title {
  font-size: var(--font-xl);
  font-weight: 700;
  display: block;
  text-align: center;
  color: var(--color-text);
}
.setup-desc {
  font-size: var(--font-sm);
  color: var(--gray-400);
  display: block;
  text-align: center;
  margin: 12rpx 0 32rpx;
}
.setup-todo {
  font-size: var(--font-sm);
  color: var(--color-warning);
  display: block;
  text-align: center;
  padding: 32rpx 0;
}
.btn-primary {
  display: block;
  padding: 24rpx;
  background: var(--blue);
  color: var(--white);
  text-align: center;
  border-radius: 12rpx;
  font-size: var(--font-md);
  font-weight: 600;
  transition: opacity var(--duration-fast);
}
.btn-primary:active { opacity: 0.85; }

/* ===== Milestone Overlay ===== */
.milestone-overlay {
  position: fixed;
  top: 0; left: 0; right: 0; bottom: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: var(--z-overlay);
  pointer-events: none;
}
.milestone-card {
  background: rgba(0,0,0,0.85);
  border-radius: var(--radius-lg);
  padding: 48rpx 56rpx;
  text-align: center;
  animation: milestoneIn var(--duration-slow) var(--easing);
}
@keyframes milestoneIn {
  from { transform: scale(0.8); opacity: 0; }
  to { transform: scale(1); opacity: 1; }
}
.milestone-icon { font-size: 80rpx; display: block; margin-bottom: 16rpx; }
.milestone-text { font-size: var(--font-lg); color: var(--white); font-weight: 600; }
.milestone-sub { font-size: var(--font-xs); color: rgba(255,255,255,0.6); margin-top: 8rpx; display: block; }

/* ===== Path Setup Steps / 路径设置步骤 ===== */
.setup-step { padding: 8rpx 0; }
.setup-q {
  font-size: var(--font-md);
  font-weight: 600;
  color: var(--color-text);
  display: block;
  margin-bottom: 24rpx;
}
.setup-option {
  padding: 24rpx 20rpx;
  margin-bottom: 12rpx;
  background: var(--gray-50);
  border-radius: 12rpx;
  font-size: var(--font-md);
  color: var(--gray-600);
  text-align: center;
  border: 2rpx solid transparent;
  transition: all var(--duration-fast) var(--easing);
}
.setup-option:active {
  background: var(--blue-light);
  border-color: var(--blue);
}
.setup-option.selected {
  background: var(--blue-light);
  border-color: var(--blue);
  color: var(--blue);
  font-weight: 600;
}
.setup-check { display: flex; align-items: center; gap: 12rpx; }
.setup-back {
  padding: 16rpx 0;
  text-align: center;
  font-size: var(--font-sm);
  color: var(--gray-400);
}
.setup-confirm { margin-top: 16rpx; }
.setup-quick {
  padding: 28rpx 0 0;
  text-align: center;
  border-top: 1rpx solid var(--gray-100);
  margin-top: 24rpx;
}
.setup-quick-text { font-size: var(--font-md); color: var(--blue); }

/* ===== Housing Wizard / 找房向导 ===== */
.housing-banner {
  margin: 16rpx;
  padding: 20rpx 28rpx;
  background: var(--gradient-warm);
  border-radius: var(--radius);
  font-size: var(--font-md);
  color: var(--color-warning);
  text-align: center;
  transition: transform var(--duration-fast);
}
.housing-banner:active { transform: scale(0.98); }
.wizard-dialog {
  width: 640rpx;
  max-height: 80vh;
  background: var(--white);
  border-radius: var(--radius-lg);
  padding: 40rpx;
  overflow-y: auto;
}
.wizard-title {
  font-size: var(--font-lg);
  font-weight: 700;
  text-align: center;
  display: block;
  margin-bottom: 24rpx;
  color: var(--color-text);
}
.wizard-q {
  font-size: var(--font-md);
  font-weight: 600;
  color: var(--gray-700);
  display: block;
  margin-bottom: 20rpx;
}
.wizard-opt {
  padding: 24rpx 20rpx;
  margin-bottom: 12rpx;
  background: var(--gray-50);
  border-radius: 12rpx;
  font-size: var(--font-md);
  text-align: center;
  border: 2rpx solid transparent;
  transition: all var(--duration-fast) var(--easing);
}
.wizard-opt:active {
  background: var(--blue-light);
  border-color: var(--blue);
}
.wizard-results { max-height: 500rpx; margin: 16rpx 0; }
.wizard-result-card {
  padding: 20rpx;
  margin-bottom: 12rpx;
  background: var(--gray-50);
  border-radius: 12rpx;
}
.result-name { font-size: var(--font-md); font-weight: 600; display: block; }
.result-stat { font-size: var(--font-xs); color: var(--gray-600); display: block; margin-top: 4rpx; }
.wizard-done {
  margin-top: 20rpx;
  padding: 20rpx;
  background: var(--blue);
  color: var(--white);
  text-align: center;
  border-radius: 12rpx;
  font-size: var(--font-md);
  font-weight: 600;
}
.wizard-close { padding: 20rpx 0 0; text-align: center; font-size: var(--font-sm); color: var(--gray-400); }
.wizard-empty { padding: 32rpx; text-align: center; font-size: var(--font-sm); color: var(--gray-400); }

/* ===== Tab 3: Articles / 攻略精选 ===== */
.article-list { height: calc(100vh - 200rpx); padding: 0 16rpx; }
.article-card {
  display: flex;
  padding: 24rpx;
  margin-bottom: 16rpx;
  background: var(--white);
  border-radius: var(--radius);
  box-shadow: var(--shadow-sm);
  transition: transform var(--duration-fast);
}
.article-card:active { transform: scale(0.98); }
.article-cover {
  width: 160rpx;
  height: 120rpx;
  border-radius: 12rpx;
  flex-shrink: 0;
  margin-right: 20rpx;
  background: var(--gray-100);
}
.article-info {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  overflow: hidden;
}
.article-title {
  font-size: var(--font-md);
  font-weight: 600;
  color: var(--gray-700);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.article-summary {
  font-size: var(--font-xs);
  color: var(--gray-400);
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}
.article-date { font-size: var(--font-xs); color: var(--gray-300); }

/* ===== Task Detail Full-Screen Modal (Tab 1 "了解详情") ===== */
.task-detail-overlay {
  align-items: flex-end;  /* Sheet slides up from bottom */
}
.task-detail-sheet {
  width: 100%;
  max-height: 90vh;
  background: var(--white);
  border-radius: 32rpx 32rpx 0 0;
  display: flex;
  flex-direction: column;
  animation: slideUp var(--duration-slow) var(--easing);
}
@keyframes slideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}
.task-detail-header {
  padding: 32rpx 32rpx 20rpx;
  border-bottom: 1rpx solid var(--gray-100);
  position: relative;
}
.task-detail-title {
  font-size: var(--font-xl);
  font-weight: 700;
  color: var(--color-text);
  display: block;
  padding-right: 60rpx;
}
.task-detail-meta {
  display: flex;
  gap: 12rpx;
  margin-top: 12rpx;
}
.task-detail-close {
  position: absolute;
  top: 32rpx;
  right: 32rpx;
  width: 56rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--gray-100);
  border-radius: 50%;
  font-size: var(--font-md);
  color: var(--gray-500);
}
.task-detail-close:active { background: var(--gray-200); }
.task-detail-body {
  flex: 1;
  padding: 24rpx 32rpx;
  max-height: 60vh;
}
.td-section {
  margin-bottom: 28rpx;
}
.td-section-title {
  font-size: var(--font-md);
  font-weight: 600;
  color: var(--color-text);
  display: block;
  margin-bottom: 12rpx;
}
.td-text {
  font-size: var(--font-sm);
  color: var(--gray-600);
  line-height: 1.7;
  display: block;
  word-break: break-word;
}
.td-step {
  display: flex;
  gap: 12rpx;
  margin-bottom: 18rpx;
}
.td-step-num {
  width: 40rpx;
  height: 40rpx;
  background: var(--blue);
  color: var(--white);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--font-xs);
  font-weight: 600;
  flex-shrink: 0;
  margin-top: 2rpx;
}
.td-step-body { flex: 1; }
.td-step-title {
  font-size: var(--font-sm);
  font-weight: 600;
  color: var(--color-text);
  display: block;
}
.td-step-content {
  font-size: var(--font-xs);
  color: var(--gray-500);
  display: block;
  margin-top: 4rpx;
  line-height: 1.6;
  word-break: break-word;
}
.td-item {
  font-size: var(--font-xs);
  color: var(--gray-500);
  padding: 4rpx 0;
  line-height: 1.6;
}
.td-renewal-hint {
  background: var(--color-warning-bg);
  border-radius: 12rpx;
  padding: 20rpx;
}
.task-detail-footer {
  padding: 20rpx 32rpx 40rpx;
  border-top: 1rpx solid var(--gray-100);
  text-align: center;
}

.wizard-school-link { margin-top: 12rpx; padding: 16rpx; text-align: center; font-size: 26rpx; color: var(--blue); border: 2rpx dashed var(--blue-light); border-radius: 12rpx; }
