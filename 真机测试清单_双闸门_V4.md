# V4 双闸门 — 真机测试清单
> 2026-05-21 | commit `6551bf7` | 5组 20条

---

## 第一组：闸门阻断（7条）

**前置：** 清除小程序数据 → 重新进入 App

| # | 操作 | 验收标准 | 不通过信号 |
|:--:|------|---------|-----------|
| 1 | 未登录 → 流程控 → 点击路径卡片 → 展开 → 点击「确认选择」 | 弹出登录半屏（🔐 登录后开启专属流程） | 直接创建流程 / 无反应 / 白屏 |
| 2 | 未登录 → 流程控 → 点击「选择流程路径」→ 选模板 | 弹出登录半屏 | 直接创建流程 |
| 3 | 未登录 → 我的 → 点击路径标签 → 进入path-select → 选路径 | 弹出登录半屏 | 直接创建流程 |
| 4 | 未登录 → 攻略书 → 流程控 → AI评估 → 结果页选路径 | Toast「请先登录后再选择路径」 | selectedPath被设置 |
| 5 | 未登录 → AI对话 → 输入消息 → AI推荐路径 → 点击采纳 | Modal「需要登录」→ 点「去登录」→ 跳转登录页 | selectedPath被设置 / 无Modal |
| 6 | 登录 → 进入status-select → 点「暂不选择」→ 回流程控 → 点路径卡片 | 弹出身份确认半屏（📝 请先确认你的身份状态） | 直接创建流程 |
| 7 | 登录+已确认身份 → 流程控 → 点路径卡片 → 点「确认选择」 | 正常创建流程，Toast「已选择：xxx」 | 半屏误弹出 / 创建失败 |

---

## 第二组：gate-sheet 交互（4条）

**前置：** 第1条操作触发的登录半屏或第6条触发的身份半屏

| # | 操作 | 验收标准 | 不通过信号 |
|:--:|------|---------|-----------|
| 8 | 登录半屏 → 点「暂不登录」 | 半屏关闭，留在流程控，流程线未创建 | 半屏不关闭 / 流程线被创建 |
| 9 | 登录半屏 → 点「微信一键登录」→ 授权 | 登录成功 → 若未选身份 → 半屏自动切换为「请先确认身份状态」 | 半屏消失 / 卡死在加载中 / 白屏 |
| 10 | 身份半屏 → 点「去确认身份」 | 跳转status-select | 不跳转 / 跳转错误页面 |
| 11 | 身份半屏 → 点「稍后再说」 | 半屏关闭，留在流程控 | 半屏不关闭 / 闪退 |

---

## 第三组：登录串联链路（3条）

| # | 操作 | 验收标准 | 不通过信号 |
|:--:|------|---------|-----------|
| 12 | 登录半屏 → 微信登录 → 身份半屏 → 去确认身份 → 在status-select选择「未申请·在职人士」→ 拍照 → 确认 → 返回 | 身份半屏自动消失 → 流程线被创建 → Toast「已选择：xxx」 | 半屏不消失 / 流程线未创建 / 页面卡在status-select |
| 13 | 同12，但在status-select再次点「暂不选择」 | 返回流程控 → 半屏不消失（保持identity模式）→ 等待用户再次操作 | 半屏消失 / 流程线被创建 |
| 14 | 模板选择 → 被闸门拦截 → 登录 → 确认身份 → 返回 | gate-passed自动重调selectTemplate → 流程线被创建 | 返回后模板面板消失 / 需手动重选 / 创建失败 |

---

## 第四组：skip-banner & UI 检查（3条）

| # | 操作 | 验收标准 | 不通过信号 |
|:--:|------|---------|-----------|
| 15 | 登录+skipped → 流程控 Tab | 页面顶部**无**「请先选择身份状态」横幅；路径列表正常展示 | skip-banner横幅仍可见 |
| 16 | 登录+skipped → 证件夹 Tab | 页面顶部**无**skip-banner横幅 | skip-banner横幅仍可见 |
| 17 | 登录+skipped → `wx.navigateTo('/pages/index/index')` → 旧首页 | 旧首页显示「请先选择身份状态」+「去选择身份」按钮 | 旧首页无skip卡片 |

---

## 第五组：边界 & 回归（3条）

| # | 操作 | 验收标准 | 不通过信号 |
|:--:|------|---------|-----------|
| 18 | 已登录+已确认身份 → 流程控 → 快速双击路径卡片「确认选择」按钮 | 仅创建**一条**流程线，无双份流程 | Storage中出现两条active流程线 |
| 19 | 飞行模式 → 流程控 → 点路径卡片 → 登录半屏 → 点「微信一键登录」 | 8秒内显示「网络异常，请稍后重试」，半屏可关闭 | 永久转圈 / 页面卡死 / 闪退 |
| 20 | 访客态 → 依次进入攻略书/证件夹/提醒器/流程控/我的各Tab → 切换后台→前台 | app无崩溃；各Tab正常渲染；无白屏 | 闪退 / 白屏 / 数据异常 |

---

## 执行记录

| 日期 | 执行人 | 通过/失败 | 备注 |
|------|--------|:--:|------|
| / | | /20 | |
